{
  "name": "AAC - Unified Roster Selection Preview (Inactive)",
  "nodes": [
    {
      "id": "aac-preview-trigger",
      "name": "Start selection preview",
      "type": "n8n-nodes-base.formTrigger",
      "typeVersion": 2.2,
      "position": [
        0,
        0
      ],
      "webhookId": "aac-roster-selection-preview",
      "parameters": {
        "authentication": "basicAuth",
        "formTitle": "Preview an article campaign",
        "formDescription": "Preview a test-only or general-dentist selection from the shared roster workbook. No emails or publishing.",
        "formFields": {
          "values": [
            {
              "fieldName": "audience",
              "fieldLabel": "Audience",
              "fieldType": "dropdown",
              "requiredField": true,
              "fieldOptions": {
                "values": [
                  {
                    "option": "Test users only"
                  },
                  {
                    "option": "General dentists (preview only)"
                  }
                ]
              },
              "defaultValue": "Test users only"
            },
            {
              "fieldName": "campaign_name",
              "fieldLabel": "Campaign name",
              "fieldType": "text",
              "requiredField": true
            },
            {
              "fieldName": "campaign_month",
              "fieldLabel": "Campaign month",
              "fieldType": "date",
              "requiredField": true
            },
            {
              "fieldName": "topic_1",
              "fieldLabel": "Topic 1",
              "fieldType": "text",
              "requiredField": true
            },
            {
              "fieldName": "topic_2",
              "fieldLabel": "Topic 2",
              "fieldType": "text",
              "requiredField": true
            },
            {
              "fieldName": "topic_3",
              "fieldLabel": "Topic 3",
              "fieldType": "text",
              "requiredField": true
            }
          ]
        },
        "responseMode": "lastNode",
        "options": {
          "path": "aac-roster-selection-preview",
          "buttonLabel": "Choose locations",
          "ignoreBots": true
        }
      }
    },
    {
      "id": "read-location-codes-and-names",
      "name": "Read location codes and names",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        260,
        0
      ],
      "parameters": {
        "documentId": {
          "__rl": true,
          "value": "1M0arM4jnZzalySGUKq60Hp1yhTDz2RQTXqMeOWfaOUA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Locations",
          "mode": "name"
        },
        "options": {
          "dataLocationOnSheet": {
            "values": {
              "rangeDefinition": "specifyRangeA1",
              "range": "A1:N250"
            }
          }
        }
      }
    },
    {
      "id": "keep-location-rows-together",
      "name": "Keep location rows together",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        520,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return [{ json: { rows: $input.all().map((item) => item.json) } }];"
      }
    },
    {
      "id": "read-public-website-urls",
      "name": "Read public website URLs",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        780,
        0
      ],
      "parameters": {
        "documentId": {
          "__rl": true,
          "value": "1M0arM4jnZzalySGUKq60Hp1yhTDz2RQTXqMeOWfaOUA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Wordpress Sites",
          "mode": "name"
        },
        "options": {
          "dataLocationOnSheet": {
            "values": {
              "rangeDefinition": "specifyRangeA1",
              "range": "A1:C250"
            }
          }
        }
      }
    },
    {
      "id": "keep-website-rows-together",
      "name": "Keep website rows together",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1040,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return [{ json: { rows: $input.all().map((item) => item.json) } }];"
      }
    },
    {
      "id": "read-general-dentist-census",
      "name": "Read general dentist census",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        1300,
        0
      ],
      "parameters": {
        "documentId": {
          "__rl": true,
          "value": "1M0arM4jnZzalySGUKq60Hp1yhTDz2RQTXqMeOWfaOUA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Doctor Census",
          "mode": "name"
        },
        "options": {
          "dataLocationOnSheet": {
            "values": {
              "rangeDefinition": "specifyRangeA1",
              "range": "A2:D250"
            }
          }
        }
      }
    },
    {
      "id": "keep-dentist-rows-together",
      "name": "Keep dentist rows together",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1560,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return [{ json: { rows: $input.all().map((item) => item.json) } }];"
      }
    },
    {
      "id": "read-test-users",
      "name": "Read test users",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        2860,
        0
      ],
      "parameters": {
        "documentId": {
          "__rl": true,
          "value": "1M0arM4jnZzalySGUKq60Hp1yhTDz2RQTXqMeOWfaOUA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Test User",
          "mode": "name"
        },
        "options": {
          "dataLocationOnSheet": {
            "values": {
              "rangeDefinition": "specifyRangeA1",
              "range": "A1:D100"
            }
          }
        }
      }
    },
    {
      "id": "validate-current-roster",
      "name": "Validate current roster",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3120,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "// Pure roster validation. Do not persist a snapshot of people or credentials.\nconst text = (value) => String(value ?? '').trim();\nconst codeFromPrimaryLocation = (value) => text(value).match(/\\(([^)]+)\\)\\s*$/)?.[1] ?? '';\nconst validEmail = (value) => /^[^\\s@]+@[^\\s@]+\\.[^\\s@]+$/.test(value);\n\nfunction siteUrl(value) {\n  const raw = text(value);\n  if (!raw) return '';\n  try {\n    const url = new URL(/^https?:\\/\\//i.test(raw) ? raw : `https://${raw}`);\n    if (url.protocol !== 'https:' || !url.hostname.includes('.') || url.username || url.password || url.search || url.hash) return '';\n    return `${url.origin}${url.pathname.replace(/\\/*$/, '/')}`;\n  } catch {\n    return '';\n  }\n}\n\nfunction buildCampaignRoster(locationRows, wordpressRows, dentistRows, testRows = []) {\n  const heldLocations = new Set(['DFW-24', 'DFW-25']);\n  const wordpressByCode = new Map();\n  for (const [rawCode, rawName, rawUrl] of wordpressRows) {\n    const code = text(rawCode);\n    if (!code) continue;\n    if (wordpressByCode.has(code)) throw new Error(`Duplicate WordPress location code: ${code}`);\n    wordpressByCode.set(code, { name: text(rawName), url: siteUrl(rawUrl) });\n  }\n\n  const locations = [];\n  const excludedLocations = [];\n  const locationByCode = new Map();\n  for (const [rawCode, rawName, rawType, rawPublicUrl] of locationRows) {\n    const code = text(rawCode);\n    if (!code) continue;\n    if (locationByCode.has(code)) throw new Error(`Duplicate location code: ${code}`);\n    const name = text(rawName);\n    const wordpress = wordpressByCode.get(code);\n    const publicUrl = siteUrl(rawPublicUrl);\n    const reason = heldLocations.has(code) ? 'not_launched'\n      : /-(E|O|P)$/i.test(code) || text(rawType) !== 'GD' ? 'specialty_or_nonpractice'\n        : /test/i.test(`${code} ${name}`) ? 'test_location'\n          : !publicUrl ? 'missing_or_invalid_public_website'\n            : !wordpress?.url ? 'missing_or_invalid_wordpress_site'\n              : wordpress.name !== name ? 'wordpress_name_mismatch' : null;\n    if (reason) {\n      excludedLocations.push({ code, name, reason });\n      continue;\n    }\n    const location = { code, name, public_website_url: publicUrl, wordpress_site_url: wordpress.url };\n    locationByCode.set(code, location);\n    locations.push(location);\n  }\n\n  const doctors = [];\n  const excludedDoctors = [];\n  const seenEmails = new Set();\n  for (const [rawName, rawPrimary, rawDepartment, rawEmployment, rawEmail] of dentistRows) {\n    const name = text(rawName);\n    if (!name) continue;\n    const primary = text(rawPrimary);\n    const code = codeFromPrimaryLocation(primary);\n    const email = text(rawEmail).toLowerCase();\n    const reason = text(rawDepartment) !== 'General Dentist' ? 'not_general_dentist'\n      : !['Full-Time', 'Part-Time'].includes(text(rawEmployment)) ? 'not_employee'\n        : /test/i.test(primary) || code === 'DFW-T' ? 'test_location'\n          : !locationByCode.has(code) ? 'location_unavailable'\n            : !validEmail(email) ? 'missing_or_invalid_work_email' : null;\n    if (reason) {\n      excludedDoctors.push({ name, code, reason });\n      continue;\n    }\n    if (seenEmails.has(email)) throw new Error(`Duplicate work email in census: ${email}`);\n    seenEmails.add(email);\n    doctors.push({ name, email, location_code: code });\n  }\n\n  const testUsers = [];\n  const seenTestEmails = new Set();\n  for (const [rawEmail, rawName, rawLocation, rawWordpressUrl] of testRows) {\n    const email = text(rawEmail).toLowerCase();\n    if (!email) continue;\n    const name = text(rawName);\n    const location = text(rawLocation);\n    const wordpressUrl = siteUrl(rawWordpressUrl);\n    if (!validEmail(email) || !name || !location || !wordpressUrl) throw new Error(`Incomplete test user row: ${email}`);\n    if (seenTestEmails.has(email)) throw new Error(`Duplicate test email: ${email}`);\n    seenTestEmails.add(email);\n    testUsers.push({ name, email, location_name: location, wordpress_site_url: wordpressUrl });\n  }\n  return { locations, doctors, testUsers, excludedLocations, excludedDoctors };\n}\n\nconst rows = (nodeName, columns) => {\n  const observed = $(nodeName).first().json.rows;\n  if (!Array.isArray(observed)) throw new Error('Sheet read unavailable: ' + nodeName);\n  return observed.map((row) => columns.map((column) => row[column] ?? ''));\n};\nconst censusRows = $('Keep dentist rows together').first().json.rows;\nconst emailByRow = new Map($('Keep email rows together').first().json.rows.map((row) => [Number(row.row_number), row['Work Email'] ?? '']));\nconst dentists = censusRows.map((row) => {\n  const rowNumber = Number(row.row_number);\n  if (!Number.isInteger(rowNumber)) throw new Error('Doctor census row number missing.');\n  return [row.Employee ?? '', row['Primary Location'] ?? '', row.Department ?? '', row['Employment Type'] ?? '', emailByRow.get(rowNumber) ?? ''];\n});\nconst roster = buildCampaignRoster(\n  rows('Keep location rows together', ['Code', 'Name', 'Type', 'URL']),\n  rows('Keep website rows together', ['Codes', 'Locations', 'Wordpress Site']),\n  dentists,\n  $('Read test users').all().map((item) => ['Email', 'Name', 'Location', 'Wordpress URL'].map((column) => item.json[column] ?? ''))\n);\nconst start = $('Start selection preview').first().json;\nconst audience = start.audience ?? start.Audience;\nif (!['Test users only', 'General dentists (preview only)'].includes(audience)) throw new Error('Choose a valid audience.');\nconst testOnly = audience === 'Test users only';\nif (testOnly && !roster.testUsers.length) throw new Error('The Test User tab has no complete entries.');\nif (!testOnly && (!roster.locations.length || !roster.doctors.length)) throw new Error('No eligible general-dentist locations or doctors.');\nconst locationByName = new Map();\nconst testLocations = [];\nfor (const user of roster.testUsers) {\n  const key = user.location_name + '|' + user.wordpress_site_url;\n  if (!locationByName.has(user.location_name)) {\n    const location = { code: 'TEST-' + (testLocations.length + 1), name: user.location_name, wordpress_site_url: user.wordpress_site_url };\n    locationByName.set(user.location_name, location);\n    testLocations.push(location);\n  } else if (locationByName.get(user.location_name).wordpress_site_url !== user.wordpress_site_url) {\n    throw new Error('Conflicting WordPress URLs for test location: ' + user.location_name);\n  }\n}\nreturn [{ json: { audience, test_only: testOnly, locations: testOnly ? testLocations : roster.locations,\n  doctors: testOnly ? roster.testUsers.map((user) => ({ name: user.name, email: user.email, location_code: locationByName.get(user.location_name).code })) : roster.doctors,\n  excluded_location_count: roster.excludedLocations.length, excluded_doctor_count: roster.excludedDoctors.length } }];"
      }
    },
    {
      "id": "choose-locations",
      "name": "Choose locations",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.2,
      "position": [
        3380,
        0
      ],
      "parameters": {
        "operation": "page",
        "defineForm": "json",
        "jsonOutput": "={{ JSON.stringify([{fieldLabel:\"Locations (all initially selected; uncheck to exclude)\",fieldName:\"selected_locations\",fieldType:\"checkbox\",fieldOptions:{values:$json.locations.map((x)=>({option:x.code+\" — \"+x.name}))},defaultValue:$json.locations.map((x)=>x.code+\" — \"+x.name),requiredField:true}]) }}",
        "options": {
          "formTitle": "Choose locations (preview only)",
          "buttonLabel": "Continue"
        }
      }
    },
    {
      "id": "filter-doctors-by-selected-locations",
      "name": "Filter doctors by selected locations",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3640,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const roster = $('Validate current roster').first().json;\nconst input = $input.first().json;\nconst raw = input.selected_locations ?? input['Locations (all initially selected; uncheck to exclude)'];\nconst selections = Array.isArray(raw) ? raw : raw ? [raw] : [];\nconst byLabel = new Map(roster.locations.map((x) => [x.code + ' — ' + x.name, x.code]));\nconst codes = selections.map((label) => {\n  const code = byLabel.get(label);\n  if (!code) throw new Error('Unknown location selection: ' + label);\n  return code;\n});\nif (!codes.length) throw new Error('Select at least one location.');\nconst chosen = new Set(codes);\nconst doctors = roster.doctors.filter((x) => chosen.has(x.location_code));\nif (!doctors.length) throw new Error('No eligible dentists at the selected locations.');\nreturn [{ json: { locations: roster.locations.filter((x) => chosen.has(x.code)), doctors } }];"
      }
    },
    {
      "id": "choose-doctors",
      "name": "Choose doctors",
      "type": "n8n-nodes-base.form",
      "typeVersion": 2.2,
      "position": [
        3900,
        0
      ],
      "parameters": {
        "operation": "page",
        "defineForm": "json",
        "jsonOutput": "={{ JSON.stringify([{fieldLabel:\"Doctors (all initially selected; uncheck to exclude)\",fieldName:\"selected_doctors\",fieldType:\"checkbox\",fieldOptions:{values:$json.doctors.map((x)=>({option:x.name+\" (\"+x.location_code+\") — \"+x.email}))},defaultValue:$json.doctors.map((x)=>x.name+\" (\"+x.location_code+\") — \"+x.email),requiredField:true}]) }}",
        "options": {
          "formTitle": "Choose recipients (preview only)",
          "buttonLabel": "Continue"
        }
      }
    },
    {
      "id": "preview-selection-without-sending",
      "name": "Preview selection without sending",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        4160,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "const available = $('Filter doctors by selected locations').first().json;\nconst input = $input.first().json;\nconst raw = input.selected_doctors ?? input['Doctors (all initially selected; uncheck to exclude)'];\nconst selections = Array.isArray(raw) ? raw : raw ? [raw] : [];\nconst byLabel = new Map(available.doctors.map((x) => [x.name + ' (' + x.location_code + ') — ' + x.email, x]));\nconst doctors = selections.map((label) => {\n  const doctor = byLabel.get(label);\n  if (!doctor) throw new Error('Unknown recipient selection: ' + label);\n  return doctor;\n});\nif (!doctors.length || new Set(doctors.map((x) => x.email)).size !== doctors.length) throw new Error('Select at least one distinct recipient.');\nconst details = $('Start selection preview').first().json;\nconst roster = $('Validate current roster').first().json;\nreturn [{ json: { preview_only: true, test_only: roster.test_only, audience: roster.audience,\n  campaign_name: details.campaign_name ?? details['Campaign name'], campaign_month: details.campaign_month ?? details['Campaign month'],\n  topics: [details.topic_1 ?? details['Topic 1'], details.topic_2 ?? details['Topic 2'], details.topic_3 ?? details['Topic 3']], location_count: available.locations.length,\n  recipient_count: doctors.length, locations: available.locations, recipients: doctors,\n  message: 'Preview only: no campaign, invitations, WordPress posts, or static-site release created.' } }];"
      }
    },
    {
      "id": "aac-preview-note",
      "name": "Selection Preview Notes",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        0,
        -380
      ],
      "parameters": {
        "content": "## Read-only campaign selection\n\nAll five readers use the Authoritative Blog Article Distribution workbook: Locations, Wordpress Sites, Doctor Census A-D and I, and Test User. Attach the existing Google Sheets credential to the readers and dedicated Basic Auth to the trigger after import. Keep inactive. Test users and general dentists are separate audiences. This workflow has no campaign write, email, WordPress publish, or static-site release action.",
        "height": 260,
        "width": 640
      }
    },
    {
      "id": "keep-email-rows-together",
      "name": "Keep email rows together",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2600,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return [{ json: { rows: $input.all().map((item) => item.json) } }];"
      }
    },
    {
      "id": "read-dentist-work-emails",
      "name": "Read dentist work emails",
      "type": "n8n-nodes-base.googleSheets",
      "typeVersion": 4.7,
      "position": [
        1820,
        0
      ],
      "parameters": {
        "documentId": {
          "__rl": true,
          "value": "1M0arM4jnZzalySGUKq60Hp1yhTDz2RQTXqMeOWfaOUA",
          "mode": "id"
        },
        "sheetName": {
          "__rl": true,
          "value": "Doctor Census",
          "mode": "name"
        },
        "options": {
          "dataLocationOnSheet": {
            "values": {
              "rangeDefinition": "specifyRangeA1",
              "range": "I2:I250"
            }
          }
        }
      }
    },
    {
      "id": "keep-email-rows-together",
      "name": "Keep email rows together",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2080,
        0
      ],
      "parameters": {
        "mode": "runOnceForAllItems",
        "jsCode": "return [{ json: { rows: $input.all().map((item) => item.json) } }];"
      }
    }
  ],
  "connections": {
    "Start selection preview": {
      "main": [
        [
          {
            "node": "Read location codes and names",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read location codes and names": {
      "main": [
        [
          {
            "node": "Keep location rows together",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Keep location rows together": {
      "main": [
        [
          {
            "node": "Read public website URLs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read public website URLs": {
      "main": [
        [
          {
            "node": "Keep website rows together",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Keep website rows together": {
      "main": [
        [
          {
            "node": "Read general dentist census",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read general dentist census": {
      "main": [
        [
          {
            "node": "Keep dentist rows together",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Keep dentist rows together": {
      "main": [
        [
          {
            "node": "Read dentist work emails",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate current roster": {
      "main": [
        [
          {
            "node": "Choose locations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Choose locations": {
      "main": [
        [
          {
            "node": "Filter doctors by selected locations",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Filter doctors by selected locations": {
      "main": [
        [
          {
            "node": "Choose doctors",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Choose doctors": {
      "main": [
        [
          {
            "node": "Preview selection without sending",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read test users": {
      "main": [
        [
          {
            "node": "Validate current roster",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Keep email rows together": {
      "main": [
        [
          {
            "node": "Read test users",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read dentist work emails": {
      "main": [
        [
          {
            "node": "Keep email rows together",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "timezone": "America/Chicago"
  },
  "active": false,
  "tags": []
}
